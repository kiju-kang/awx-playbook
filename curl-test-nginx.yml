- hosts: all
  gather_facts: no
  connection: local
  
  tasks:
  - name: Check that you can connect (GET) to a page and it returns a status 200
    uri:
      url: https://google.com
      register: rs

  - debug:
      var: rs
